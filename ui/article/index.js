import styled from 'styled-components';
import {
  color,
  space,
  width,
  height,
  fontSize,
  maxWidth,
  textAlign,
  lineHeight
} from 'styled-system';
// //////////////////////////////////////////////////

export const Article = styled.article`
  ${space}
  ${width}
  ${color}
  ${height}
  ${maxWidth}
  ${fontSize}
  ${textAlign}
  ${lineHeight}
`;
