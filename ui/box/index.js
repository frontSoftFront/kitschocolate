import styled from 'styled-components';
import {
  space,
  width,
  order,
  height,
  border,
  display,
  borderTop,
  borderLeft,
  borderRight,
  borderBottom,
  borderRadius
} from 'styled-system';
// //////////////////////////////////////////////////

export const Box = styled.div`
  ${space}
  ${width}
  ${order}
  ${height}
  ${border}
  ${display}
  ${borderTop}
  ${borderLeft}
  ${borderRight}
  ${borderBottom}
  ${borderRadius}
`;
