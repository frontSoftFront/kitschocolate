import styled from 'styled-components';
import {
  space,
  width,
  color,
  fontSize,
  maxWidth,
  alignItems,
  flexDirection,
  justifyContent
} from 'styled-system';
// //////////////////////////////////////////////////

export const Nav = styled.nav`
  ${space}
  ${width}
  ${color}
  ${fontSize}
  ${maxWidth}
  ${alignItems}
  ${flexDirection}
  ${justifyContent}
  display: flex;
`;
